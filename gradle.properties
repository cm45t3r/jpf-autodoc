# Gradle properties for jpf-autodoc
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# Gradle 9.0 compatibility - require JVM 17+ for Gradle daemon
# This ensures compatibility with Gradle 9.0 while allowing projects to use older JVMs via toolchains
# Note: Install Java 17+ and set JAVA_HOME or use sdkman/homebrew to install openjdk@17
# org.gradle.java.home=

# Project properties
project.name=jpf-autodoc
project.version=1.0.0-SNAPSHOT
project.description=Unified JPF documentation generation tool 